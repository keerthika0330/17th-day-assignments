#include <stdio.h>
 main()
  {
    int i, n, r, sum = 0;
    scanf("%d", &i);
    n = i;
    while (n!= 0)
	 {
        r = n%10;
		sum += r*r*r;
	    n =n/10;
    }
    if (sum == i)
		 printf("It is an Armstrong number.", i);
		 else
		 printf("It is not an Armstrong number.",i);
}
