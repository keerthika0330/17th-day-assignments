#include <stdio.h>
int main() {
  int n, r= 0,i, k;
    printf("Enter an integer: ");
    scanf("%d", &n);
     k = n;
    while (n != 0) 
	{
       i = n % 10;
        r = r * 10 +i;
        n =n/10;
    }
    if (k == r)
        printf("%d is a palindrome.", k);
   
    return 0;
}
